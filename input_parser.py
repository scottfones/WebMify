import re

from pathlib import Path


def parse(raw_input):
    parsed_list = []
    parsed_title = ''
    parsed_season_num = ''
    parsed_ep_num = ''

    return parsed_list, parsed_title, parsed_season_num, parsed ep_num
